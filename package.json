{
  "name": "oig-cloud",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "echo \"V1 dashboard retired. Frontend now lives in custom_components/oig_cloud/www_v2 (self-linted, type-checked and built in build-frontend.yml). Nothing to lint at repo root.\" && exit 0"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@vitest/coverage-v8": "^4.0.17",
    "baseline-browser-mapping": "^2.9.14",
    "eslint": "^8.57.0",
    "htmlhint": "^1.1.4",
    "jsdom": "^24.0.0",
    "stylelint": "^16.0.0",
    "stylelint-config-standard": "^36.0.0",
    "vitest": "^4.0.17"
  }
}
