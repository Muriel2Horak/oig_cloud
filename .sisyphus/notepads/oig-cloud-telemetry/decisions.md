## 2026-04-20 - Task 0
- Record the IP-based Grafana URL (`http://10.0.0.161:3000/login`) as the Task 12 browser target because the `docker-vm` hostname does not resolve from the agent environment.
- Use the repo virtualenv (`/repos/oig-cloud/.venv/bin`) for baseline commands; the system interpreter lacked project test dependencies and produced collection failures before the venv-backed run.
- Treat Loki rendered-text proof as explicitly deferred instead of fabricating synthetic log injection, matching the plan's requirement for a supported warning path only.
- 2026-04-20: Kept MQTT delivery inert by default with `telemetry_mqtt_enabled = false`, empty host, port `1883`, and prefix `oig/cloud-telemetry`, so no broker interaction is implied unless the user explicitly enables and configures it.
- 2026-04-20: Added MQTT fields directly to the wizard modules step instead of creating a separate step to match the existing flow structure and the task requirement.
- 2026-04-20: Task 1 freezes the cloud telemetry contract in `shared/cloud_contract.py` with strict producer validation, sink-side transport injection, per-event-class allowlists, and template-generated `detail_failure_summary` / `detail_error_summary` values derived only from exception class names.
- 2026-04-20: Task 11 keeps proxy consumers untouched and adds a dedicated cloud `mqtt_consumer` on `oig/cloud-telemetry/#` with `json_v2`, explicit tag/field declarations, `timestamp_path = 'occurred_at'`, and `topic_tag = ''` so `device_id` remains the only identifier tag on cloud points.
- 2026-04-20: Task 6 keeps `details_json` as the flat string field at the event boundary, but sanitizes nested dict inputs first by dropping forbidden entity/label/name keys, rejecting HA-state-like payloads and unsupported structures, and enforcing the 2 KiB cap after serialization.
- 2026-04-20: Task 3 keeps the MQTT adapter fail-open and enqueue-only, but now accepts either a serialized JSON payload or a mapping, extracting the routing `device_id` from the payload itself before enqueue.
- 2026-04-20: Task 3 normalizes the publish namespace to the fixed cloud topic family `oig/cloud-telemetry/<device_id>` and rejects non-numeric `device_id` values so sink routing stays aligned with the box-id contract.
- 2026-04-20: Task 4 stores all shared telemetry state under `hass.data[DOMAIN][entry.entry_id]["telemetry"]` as one dict containing the lazy `SharedTelemetryEmitter`, optional MQTT publisher/sink, and optional New Relic handler/sink; no root-level `hass.data[DOMAIN]["telemetry"]` state is used.
- 2026-04-20: `SharedTelemetryEmitter.emit_cloud_event()` validates producer events once via `validate_producer_event()` and injects `transport` separately per sink with `build_sink_payload()`, while `emit_raw_event()` bypasses taxonomy validation and routes only to the New Relic sink.
- 2026-04-20: Shield now binds its telemetry emitter inside `_setup_service_shield_data()` from the entry-scoped telemetry state before the legacy global `hass.data[DOMAIN]["shield"]` sensor pointer is refreshed; the global pointer remains sensor-only and is never used as telemetry state.
- 2026-04-20: Removed the unused integration-level `_setup_telemetry()` path from `__init__.py` and the direct SimpleTelemetry setup from `ServiceShield.__init__()`, leaving the shared emitter as the only Shield telemetry transport path.
- 2026-04-20: Task 9b keeps the emission boundary inside `DataSourceController._on_effective_mode_changed()`, derives the transition direction from the prior persisted `DataSourceState`, and skips incident emission entirely when no prior state exists yet.
- 2026-04-20: Task 9b stores fallback/recovery dedupe under `hass.data[DOMAIN][entry_id]["telemetry"]["incident_dedupe"]`, reuses `build_producer_event()` with `detail_data_source_from/to` set to the real `cloud_only` / `local_only` constants, and resets the opposite incident family on each successful emit.
