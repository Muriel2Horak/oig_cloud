/**
 * ČHMÚ UI types — re-exports from data layer for component use.
 */
export type {
  ChmuData,
  ChmuWarningDetail,
} from '@/data/chmu-data';

export {
  EMPTY_CHMU_DATA,
  getChmuIcon,
  SEVERITY_LABELS,
  SEVERITY_COLORS,
} from '@/data/chmu-data';
