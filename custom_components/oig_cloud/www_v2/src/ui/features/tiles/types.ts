/**
 * Tiles UI types — re-exports from data layer for component use.
 */
export type {
  TileConfig,
  TilesConfig,
  ResolvedTile,
  TileSupportEntities,
} from '@/data/tiles-data';

export { DEFAULT_TILES_CONFIG } from '@/data/tiles-data';
