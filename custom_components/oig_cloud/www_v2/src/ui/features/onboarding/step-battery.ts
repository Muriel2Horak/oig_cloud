import type { WizardStep } from '@/ui/features/onboarding/step-solar';

/** Stub only — Stage S3 Task 17 fills in the registry-driven body. */
export const STEP_BATTERY: WizardStep = {
  id: 'battery',
  blocksDashboard: false,
  skippable: true,
};
