import type { WizardStep } from '@/ui/features/onboarding/step-solar';

/** Stub only — Stage S3 Task 19 fills in the registry-driven body. */
export const STEP_CONNECTION: WizardStep = {
  id: 'connection',
  blocksDashboard: false,
  skippable: true,
};
