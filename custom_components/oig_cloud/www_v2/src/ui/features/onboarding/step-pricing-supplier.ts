import type { WizardStep } from '@/ui/features/onboarding/step-solar';

/** Stub only — Stage S3 Task 16 fills in the registry-driven body. */
export const STEP_PRICING_SUPPLIER: WizardStep = {
  id: 'pricing_supplier',
  blocksDashboard: false,
  skippable: true,
};
