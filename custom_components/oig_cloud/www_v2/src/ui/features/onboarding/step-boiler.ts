import type { WizardStep } from '@/ui/features/onboarding/step-solar';

/** Stub only — Stage S3 Task 18 fills in the registry-driven body. */
export const STEP_BOILER: WizardStep = {
  id: 'boiler',
  blocksDashboard: false,
  skippable: true,
};
