CRITICAL: 0 | MAJOR: 1 | MINOR: 0

# R10-AIKEYS round 5

## 1. Prior-finding verdicts

Verdict scope: CLOSED means the updated documents add a binding requirement or correctly route an already-shipped defect to `spec-critique/SHIPPED-CODE-DEFECTS.md`. CLOSED does not claim shipped code is fixed. Prior defect rows closed: 17/18. Prior defect rows open: 1/18.

| id | CLOSED/PARTIAL/OPEN | evidence `file:clause` |
|---|---|---|
| AIK-1 / R2-S1 - no `AiKeyStore` delete or integration teardown cleanup | CLOSED | `SCOPE-REVISION.md:R6.7` requires delete/clear semantics and integration removal cleanup for `oig_cloud.ai_<entry_id>` (`SCOPE-REVISION.md:210`). `SCOPE-REVISION.md:R8.7` names the exact AI store, `private=True`, and clear/provider-switch/remove deletion assertions (`SCOPE-REVISION.md:368`). SHIPPED-CODE routing remains correct: `spec-critique/SHIPPED-CODE-DEFECTS.md:11`. |
| AIK-2 / R2-S2 - provider change can keep a stale AI key | CLOSED | `SCOPE-REVISION.md:R6.7` requires options flow not to keep a mismatched key/provider pair (`SCOPE-REVISION.md:210`). `SCOPE-REVISION.md:R8.7` requires provider-switch deletion of `oig_cloud.ai_<entry_id>` and `oig_cloud.solar_<entry_id>` (`SCOPE-REVISION.md:373`). SHIPPED-CODE routing remains correct: `spec-critique/SHIPPED-CODE-DEFECTS.md:17`. |
| AIK-3 / R2-S3 - `/ai` stores replacement before verification | CLOSED | `SCOPE-REVISION.md:R6.7` requires key replacement to verify before active-key switch (`SCOPE-REVISION.md:212`). SHIPPED-CODE routing remains correct: `spec-critique/SHIPPED-CODE-DEFECTS.md:23`. |
| AIK-4 / R2-T2 - raw verify exception detail can reach REST/UI | CLOSED | `SCOPE-REVISION.md:R6.9` requires failing verify checks to keep provider key text out of REST payloads and diagnostics (`SCOPE-REVISION.md:220`). SHIPPED-CODE routing remains correct for the current raw-detail path: `spec-critique/SHIPPED-CODE-DEFECTS.md:29`. |
| AIK-5 / R2-T3 - prompt `task` is free text | CLOSED | `SCOPE-REVISION.md:R6.8` requires `task` to be enum/constant and forbids raw `task` concatenation (`SCOPE-REVISION.md:215`). SHIPPED-CODE routing remains correct: `spec-critique/SHIPPED-CODE-DEFECTS.md:35`. |
| AIK-6 / R2-P2 - `validate_config` lacks typed anonymous collector and wire test | CLOSED | `SCOPE-REVISION.md:R6.8` requires collectors to exclude coordinates, `box_id`, `entity_id`, API keys, and PII, with a wire-payload falsifier (`SCOPE-REVISION.md:215`). `SCOPE-REVISION.md:R8.8` also excludes `solcast_site_id` from the AI prompt collector (`SCOPE-REVISION.md:378`). SPEC routing remains correct: `spec-critique/R6-CLASSIFICATION.md:67`. |
| AIK-7 / R2-P3 - `ai_task` delegation branch unverified | CLOSED | The defect is correctly routed to shipped code with runtime verification required before use (`spec-critique/SHIPPED-CODE-DEFECTS.md:41`). The spec-side prompt boundary is covered by `SCOPE-REVISION.md:R6.8` (`SCOPE-REVISION.md:215`). |
| R2-A3 - `oig_ai_status` sensor privacy boundary | CLOSED | `SCOPE-REVISION.md:R6.9` requires `oig_ai_status` and diagnostics to avoid raw key material, secret predicates, and unredacted raw exceptions (`SCOPE-REVISION.md:220`). The implementation handoff repeats the diagnostic redaction rule (`docs/redesign_2026_07/IMPLEMENTATION-BRIEF-EN.md:94`). |
| R2 planned-risk row - cross-provider fallback consent | CLOSED | `SCOPE-REVISION.md:R7.11` keeps fallback within the selected provider by default, requires explicit stored consent for `ai_task` to backend and Groq to NVIDIA crossings, and requires no outbound call without consent (`SCOPE-REVISION.md:301`). |
| AKEY-R6-001 - `/solar_test` secrecy | CLOSED | `SCOPE-REVISION.md:R7.3` binds logs, REST payloads, diagnostics, and DOM-visible errors to classified-only output for Forecast.Solar and Solcast and bans secret text, request URLs, request bodies, and raw exceptions (`SCOPE-REVISION.md:253`). `SCOPE-REVISION.md:R8.8` extends the same lifecycle to `solcast_site_id` (`SCOPE-REVISION.md:378`). |
| AKEY-R6-002 - solar credential private storage, clear, teardown, and backup hygiene | CLOSED | `SCOPE-REVISION.md:R7.1` requires solar secret fields to live only in per-entry private stores and out of `entry.options` except metadata and `*_set` booleans (`SCOPE-REVISION.md:240`). `SCOPE-REVISION.md:R7.2` fail-closes backup, journal, audit, and diagnostics writes containing secret keys or values (`SCOPE-REVISION.md:247`). `SCOPE-REVISION.md:R8.7` names `oig_cloud.solar_<entry_id>` and teardown semantics (`SCOPE-REVISION.md:368`). |
| AKEY-R6-003 - replacement solar key verified before activation | CLOSED | `SCOPE-REVISION.md:R7.12` requires a new solar key to pass `/solar_test` before activation, preserves the old credential on failed/skipped verification, and fails provider switches without verified replacement material (`SCOPE-REVISION.md:307`). `PLAN-3.6-SPEC.md` binds AK-2 to R7.12 and fails if a replacement key is sent without successful `/solar_test` (`docs/redesign_2026_07/PLAN-3.6-SPEC.md:52`). |
| AKEY-R6-004 - explicit cross-provider fallback consent | CLOSED | `SCOPE-REVISION.md:R7.11` defines the consent boundary and seeded falsifier: missing consent plus `ai_task` failure must make no outbound Groq/NVIDIA call and must surface a refusal code (`SCOPE-REVISION.md:301`). |
| AKEY-R7-001 - R7.7 secret-remount contradiction | CLOSED | `SCOPE-REVISION.md:R8.1` supersedes the reversed R7.7/R7.8 falsifiers, requires non-secret Step-2 values on remount, forbids raw solar secret fields in UI, `entry.options`, `/onboarding` request/response, and saved draft state, and allows only `*_set` booleans plus blank password controls (`SCOPE-REVISION.md:319`). The handoff repeats the polarity override (`docs/redesign_2026_07/IMPLEMENTATION-BRIEF-EN.md:165`). |
| AKEY-R7-002 - exact credential store names and teardown semantics | CLOSED | `SCOPE-REVISION.md:R8.7` binds exact store names `oig_cloud.ai_<entry_id>` and `oig_cloud.solar_<entry_id>`, requires `private=True`, forbids collision with `oig_cloud.migration_backup_<entry_id>`, and requires clear/provider-switch/entry-removal deletion assertions (`SCOPE-REVISION.md:368`). `SCOPE-REVISION.md:R9.3` adds a teardown budget and asserts both stores are gone (`SCOPE-REVISION.md:426`). |
| AKEY-R7-003 - `solcast_site_id` classification | CLOSED | `SCOPE-REVISION.md:R8.8` classifies `solcast_site_id` as a sensitive account/site identifier and binds it to private storage, `/solar_test` redaction, AI prompt exclusion, diagnostics/log redaction, and raw URL exclusion (`SCOPE-REVISION.md:378`). `SCOPE-REVISION.md:R9.1` also forbids `solcast_site_id` in non-admin `/onboarding` refusal outputs and admin positive raw-value outputs (`SCOPE-REVISION.md:411`). |
| AKEY-R7-004 - wrong AK-2 citation to R7.11 instead of R7.12 | CLOSED | `SCOPE-REVISION.md:R8.9` requires AK-2 to cite R7.3 and R7.12 and requires classified Step-2 failure not to block `wizard-next` or `wizard-skip` (`SCOPE-REVISION.md:385`). The edited plan cites R7.12 on the verification-before-replace line (`docs/redesign_2026_07/PLAN-3.6-SPEC.md:52`). |
| R9-AKEY-001 - closed `/api/oig_cloud/**` matrix omits `/api/oig_cloud/{box}/ai` | OPEN | `SCOPE-REVISION.md:R9.1` still says every shipped endpoint under `/api/oig_cloud/**` must appear in the closed matrix (`SCOPE-REVISION.md:399`), but the matrix still lists only `/module_config`, `/config_registry`, `/pricelists`, `/solar_test`, and `/onboarding` (`SCOPE-REVISION.md:403`). `PLAN-3.6-SPEC.md:R9 closeout bindings` repeats the same omission (`docs/redesign_2026_07/PLAN-3.6-SPEC.md:122`). `IMPLEMENTATION-BRIEF-EN.md:R9.1` repeats the same omission (`docs/redesign_2026_07/IMPLEMENTATION-BRIEF-EN.md:176`). The shipped AI endpoint still exists and is registered: `custom_components/oig_cloud/api/ha_rest_api.py:1330`, `custom_components/oig_cloud/api/ha_rest_api.py:1553`. |

Neutral R2 verification rows (`R2-S4`, `R2-T1`, `R2-A1`, `R2-P1`) remain non-defect observations.

## 2. New findings

| id | severity | `file:clause` | what is wrong | the falsification test that would catch it | the exact spec text that would fix it |
|---|---|---|---|---|---|
| R10-AKEY-001 | MAJOR | `SCOPE-REVISION.md:R10.3` (`SCOPE-REVISION.md:446`), `docs/redesign_2026_07/IMPLEMENTATION-BRIEF-EN.md:R9.2` (`docs/redesign_2026_07/IMPLEMENTATION-BRIEF-EN.md:194`), `custom_components/oig_cloud/api/ha_rest_api.py:1330` | R10 hardens provider-consumption concurrency for `/solar_test` only. The AI key verification route `/api/oig_cloud/{box}/ai` is another provider-consuming verification path: `POST` accepts `provider` and `api_key`, constructs `OpenAiCompatBackend`, and runs `async_verify_key` (`custom_components/oig_cloud/api/ha_rest_api.py:1360`, `custom_components/oig_cloud/api/ha_rest_api.py:1405`, `custom_components/oig_cloud/api/ha_rest_api.py:1412`). An implementer can satisfy the new R10.3 body-hash/concurrency tests while leaving AI key verification outside any one-in-flight, one-per-30s, four-total provider-call bound. This is MAJOR, not CRITICAL, because it does not newly permit unprotected key persistence/logging/transmission, but it leaves the AI key verification surface unbounded and outside the same provider-consumption control. | Add `/api/oig_cloud/{box}/ai` to the provider-consumption stress test. As admin, send 100 valid-prefix but distinct `api_key` bodies for one entry/provider in one second, then repeat across Groq and NVIDIA. Assert maximum provider verification concurrency is one per `(entry_id, provider)`, total integration provider concurrency is at most four, excess requests return classified `rate_limited` before `OpenAiCompatBackend` or the shared aiohttp session is called, and REST/DOM/logs/diagnostics contain no raw key, masked key tail, request body, `Authorization` header, provider URL, or raw upstream exception. | Add to `R10.3`: "`/api/oig_cloud/{box}/ai` verification POSTs share the same provider-consumption control as `/solar_test`: the concurrency and rate bucket key is exactly normalized `(entry_id, provider)`. API-key or request-body hashes MAY be used only for response deduplication and MUST NOT create independent rate or concurrency buckets. At most one AI verify call may be in flight per `(entry_id, provider)`, at most four provider calls may be in flight across the integration, and at most one outbound AI verify call per `(entry_id, provider)` per `30 s` is allowed. Excess AI verify work MUST be rejected as classified `rate_limited` before constructing the provider backend or calling the shared aiohttp session. Failure handling MUST be classified-only in REST, DOM, logs, and diagnostics and MUST NOT include raw key text, masked key tail, request body, `Authorization` header, provider URL carrying key material, or raw upstream exception string." |

## 3. Bucket routing

| bucket | findings |
|---|---|
| SPEC | Open prior: `R9-AKEY-001`. New: `R10-AKEY-001`. Prior SPEC closures: `AIK-6`, `R2-A3`, `R2 planned-risk row`, `AKEY-R6-001`, `AKEY-R6-002`, `AKEY-R6-003`, `AKEY-R6-004`, `AKEY-R7-001`, `AKEY-R7-002`, `AKEY-R7-003`, `AKEY-R7-004`. |
| SHIPPED-CODE | Prior routed defects only: `AIK-1`, `AIK-2`, `AIK-3`, `AIK-4`, `AIK-5`, `AIK-7` (`spec-critique/SHIPPED-CODE-DEFECTS.md:11`). No new SHIPPED-CODE finding in this round. |

## 4. What I could not establish

- I did not establish future runtime behavior for the R10 fixes; this is a spec review. Current code was inspected only to prove the existing `/api/oig_cloud/{box}/ai` route and provider-verify path are shipped surfaces (`custom_components/oig_cloud/api/ha_rest_api.py:1330`, `custom_components/oig_cloud/api/ha_rest_api.py:1360`, `custom_components/oig_cloud/api/ha_rest_api.py:1553`).
- I did not run a build or test suite, per brief.
- I did not edit `SCOPE-REVISION.md`, `PLAN-3.6-SPEC.md`, `IMPLEMENTATION-BRIEF-EN.md`, shipped code, or any file other than this report.
