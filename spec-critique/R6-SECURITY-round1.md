# R6 security re-critique - CRITICAL: 2 / MAJOR: 2 / MINOR: 0

## 1. Prior-finding verdicts

| id | verdict | evidence |
|---|---|---|
| C-1 | CLOSED | `SCOPE-REVISION.md:R6.1` now requires `Store(..., private=True)`, removal of `_SECRET_FIELDS` before migration backup write, fail-closed behavior, and a seed-key falsification test. |
| C-2 | PARTIALLY-CLOSED | Routing confirmed: `R6-CLASSIFICATION.md:SEC-2` sends the current non-admin `module_config` GET hole to `SHIPPED-CODE-DEFECTS.md:SEC-2`. The spec path exists (`SCOPE-REVISION.md:40` and Plan 4 Task 1), but this is not closed by R6 itself. |
| M-1 | CLOSED | `SCOPE-REVISION.md:R6.5` now requires `POST /api/oig_cloud/{box}/solar_test` to be admin-authenticated, bounded by timeout, rate-limited, and to return classified safe errors; `PLAN-3.6-SPEC.md:AK-2` binds the wizard contract to R6.5. |
| M-2 | OPEN | `SCOPE-REVISION.md:R5.2` says authenticated `GET /pricelists`; `SCOPE-REVISION.md:R6.3` defines returned data but still does not decide admin vs non-admin or require a non-admin refusal test. See R6-SEC-3. |
| M-3 | CLOSED | Plan 4 Task 2 now contains a restore test asserting `_migration` is absent after restore, and R6 maps the rollback invariant through `SCOPE-REVISION.md:R6.1` and `R6.11`. |
| M-4 | PARTIALLY-CLOSED | Secret persistence in the shared migration/dead-key backup path is covered by `SCOPE-REVISION.md:R6.1`; the shared-store overwrite/coupling risk is still not explicitly closed or locked. |
| M-5 | PARTIALLY-CLOSED | `SCOPE-REVISION.md:R6.1` closes migration-backup persistence of option secrets, but R6 does not forbid primary solar API-key persistence in `entry.options` or define solar-key delete/rotation. See R6-SEC-1. |
| M-6 | CLOSED | `SCOPE-REVISION.md:R6.9` requires `oig_ai_status` and diagnostics to avoid raw key material, secret predicates, and raw provider exceptions, with a failing-verify falsification test. |
| m-1 | CLOSED | Detached release signing is rejected as mandatory for this scope: `SCOPE-REVISION.md:R6.2` requires byte-equivalent CI comparison between script output and checked-in release bytes, plus hard failure for the empty-stub attack. |
| m-2 | PARTIALLY-CLOSED | `SCOPE-REVISION.md:R4` and `R5.1` keep build dependencies out of the integration manifest and require a separate build requirement surface, but R6 still does not name the exact file path or lock policy. |
| m-3 | PARTIALLY-CLOSED | `SCOPE-REVISION.md:R6.1` prevents backup persistence of option secrets, but the updated text still lacks a general rule that pre-seed/cleanup transforms cannot write secret-bearing options. See R6-SEC-1. |
| m-4 | PARTIALLY-CLOSED | Plan 4 adds an admin restore service, but neither R6 nor the plan adds a confirm parameter or durable audit fields for restore invocation. |
| m-5 | PARTIALLY-CLOSED | Routing confirmed: `R6-CLASSIFICATION.md:AIK-7` sends the unverified host `ai_task` branch to shipped-code follow-up; R6.9/R6.12 do not themselves verify that runtime branch. |

## 2. New findings

| id | severity | file:clause | attack scenario | test that catches it | exact spec text that would fix it |
|---|---|---|---|---|---|
| R6-SEC-1 | CRITICAL | `SCOPE-REVISION.md:R6.1`, `SCOPE-REVISION.md:R6.7`, `PLAN-3.6-SPEC.md:AK-2` | Admin enters `solar_forecast_api_key=leak-me` in wizard step 2 or `/module_config`; updated R6 protects migration backups and AI keys, but does not require solar forecast secrets to leave `entry.options`, so the key can remain in config-entry storage, diagnostics, or inactive-provider state after a provider switch. | Fill step 2 with a forecast key, save, switch provider to `solcast` without a replacement forecast key, remove the integration, then assert `entry.options`, diagnostics, migration backups, and non-private stores never contain `leak-me`; only a per-entry private credential store may contain it before explicit clear/removal. | Add: `All fields where FIELD_REGISTRY[field].secret is true, including solar_forecast_api_key and solcast_api_key, MUST be persisted only in per-entry Store(..., private=True). Config-entry options may persist only non-secret metadata and *_set booleans. Provider switch without a replacement key MUST either fail with explicit user action or clear the inactive provider secret. Integration removal MUST clear every per-entry credential store and backup copy.` |
| R6-SEC-2 | CRITICAL | `SCOPE-REVISION.md:R6.1`, `SCOPE-REVISION.md:R6.9`, `docs/redesign_2026_07/plans/2026-07-17-f1-plan4-cleanup-migration-dataset.md@codex/f1-spec-complete:Task 2` | A migration transform raises `ValueError(snapshot)` or a future secret field is added to `FIELD_REGISTRY` but not `_SECRET_FIELDS`; Plan 4's copyable `_backup_store` snippet still omits `private=True`, and R6 only names `_SECRET_FIELDS`, so journal/error/audit payloads can persist a secret value even if the snapshot keys are stripped. | Seed `solar_forecast_api_key=leak-me` and a registry-only `Field(secret=True)` value, force a transform failure whose exception includes the snapshot, then assert the serialized backup, journal, audit/log capture, and diagnostics contain neither secret keys nor `leak-me`; also assert every `config_migration` Store construction is private. | Add: `The migration secret set MUST be derived from FIELD_REGISTRY entries with secret=True plus legacy aliases, not from _SECRET_FIELDS alone. Before every backup, removed_keys, journal, audit, or diagnostics write, serialize the candidate payload and fail closed if it contains any secret key or any current secret value. Journal and audit records MUST store enum error codes only, never raw str(err). Plan 4 config_migration snippets MUST use Store(..., private=True).` |
| R6-SEC-3 | MAJOR | `SCOPE-REVISION.md:R5.2`, `SCOPE-REVISION.md:R6.3`, `SCOPE-REVISION.md:R6.11` | An authenticated non-admin enumerates `/api/oig_cloud/{box}/config_registry` or `/api/oig_cloud/{box}/pricelists` across guessed box IDs; the updated text states data shape and cache discipline but not the auth matrix or non-admin refusal tests for all box-scoped endpoints. | For `/module_config`, `/config_registry`, `/pricelists`, and `/solar_test`, run authenticated non-admin GET/POST/PUT/DELETE as applicable; assert 403 for every admin-scoped route, 405 for unsupported methods, and no response distinguishes existing vs missing `box_id` for non-admins. | Add an endpoint auth matrix: `/module_config` GET/POST admin-only; `/solar_test` POST admin-only and no GET; `/config_registry` GET admin-only and no write routes; `/pricelists` GET admin-only or move to a boxless public route, with no write routes. Every row MUST have an authenticated non-admin refusal test and unsupported-method test. |
| R6-SEC-4 | MAJOR | `SCOPE-REVISION.md:R6.5`, `PLAN-3.6-SPEC.md:AK-2` | Admin posts `/solar_test` with extra fields such as `entity_id`, `box_id`, `callback_url`, `provider_base_url`, or an unexpected API-key field; because R6.5 says "current form values" but does not require a closed request schema, an implementation can forward the raw body to a forecast provider, diagnostics, or logs. | POST `/solar_test` with valid provider fields plus `entity_id`, `box_id`, `callback_url`, and `provider_base_url`; assert 400 before any outbound call. In the success test, the provider stub must receive only the provider-specific allow-listed fields. | Add: `The /solar_test request body MUST be provider-specific JSON Schema with additionalProperties:false. Accepted fields are exactly provider, the selected provider credential or site id, latitude, longitude, and active string kWp/declination/azimuth values. Reject box_id, entity_id, URLs/base_url, and any unknown key before outbound calls. Never forward the raw request body to providers, diagnostics, or logs.` |

## 3. Could not establish

- D8 AI implementation plan was not present in the requested read set. I verified only the R6 prompt-boundary requirements, not a concrete future D8 implementation.
- No slow build or test gate was run, per brief. Evidence is from document reads, `git show`, and targeted grep/code inspection only.
- R6 is binding, but the older Plan 4 snippets remain stale in places. I could not establish that implementer briefs will update those snippets instead of copying them.
