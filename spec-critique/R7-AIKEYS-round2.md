# R7-AIKEYS round 2 - CRITICAL 1 / MAJOR 2 / MINOR 1

## 1. Prior-finding verdicts

Verdict scope: CLOSED means the updated documents either add a binding requirement or correctly route an already-shipped defect to `SHIPPED-CODE-DEFECTS.md`. It does not claim the shipped code is fixed.

| id | CLOSED/PARTIAL/OPEN | evidence |
|---|---|---|
| AIK-1 / R2-S1 - no `AiKeyStore` delete or integration teardown cleanup | CLOSED | `SCOPE-REVISION.md:R6.7` requires delete/clear semantics and integration removal cleanup for `oig_cloud.ai_<entry_id>` (`SCOPE-REVISION.md:210-213`). SHIPPED-CODE routing is confirmed by `R6-CLASSIFICATION.md:62` and `SHIPPED-CODE-DEFECTS.md:11-15`; current store still has set/get/mark/state only (`custom_components/oig_cloud/ai/key_store.py:43-67`). |
| AIK-2 / R2-S2 - provider change can keep a stale AI key | CLOSED | `SCOPE-REVISION.md:R6.7` requires options flow to fail rather than silently keep a mismatched key/provider pair (`SCOPE-REVISION.md:210-213`). SHIPPED-CODE routing is confirmed by `R6-CLASSIFICATION.md:63` and `SHIPPED-CODE-DEFECTS.md:17-21`; current options flow still writes a new key only when nonblank and still updates provider/options (`custom_components/oig_cloud/config/steps.py:3599-3612`). |
| AIK-3 / R2-S3 - `/ai` stores replacement before verification | CLOSED | `SCOPE-REVISION.md:R6.7` requires verification before active-key switch (`SCOPE-REVISION.md:210-213`). SHIPPED-CODE routing is confirmed by `R6-CLASSIFICATION.md:64` and `SHIPPED-CODE-DEFECTS.md:23-27`; current REST path still calls `async_set_key` before `async_verify_key` (`custom_components/oig_cloud/api/ha_rest_api.py:1394-1424`). |
| AIK-4 / R2-T2 - raw verify exception detail can reach REST/UI | CLOSED | `SCOPE-REVISION.md:R6.9` bans unredacted raw provider exceptions in execution/diagnostics (`SCOPE-REVISION.md:220-222`). SHIPPED-CODE routing is confirmed by `R6-CLASSIFICATION.md:65` and `SHIPPED-CODE-DEFECTS.md:29-33`; current response still returns `detail: str(err)` (`custom_components/oig_cloud/api/ha_rest_api.py:1411-1420`). |
| AIK-5 / R2-T3 - prompt `task` is free text | CLOSED | `SCOPE-REVISION.md:R6.8` requires `task` to be enum/constant, never concatenated raw text (`SCOPE-REVISION.md:215-218`). SHIPPED-CODE routing is confirmed by `R6-CLASSIFICATION.md:66` and `SHIPPED-CODE-DEFECTS.md:35-39`; current prompt still prepends `task={task}` (`custom_components/oig_cloud/ai/backends.py:60-72`). |
| AIK-6 / R2-P2 - `validate_config` lacks typed anonymous collector and wire test | CLOSED | `SCOPE-REVISION.md:R6.8` requires validate-config collectors to exclude coordinates, `box_id`, `entity_id`, API keys and PII, with a wire-payload falsification test (`SCOPE-REVISION.md:215-218`). SPEC routing in `R6-CLASSIFICATION.md:67` is confirmed. |
| AIK-7 / R2-P3 - `ai_task` delegation branch unverified | CLOSED | SHIPPED-CODE routing is confirmed by `R6-CLASSIFICATION.md:68` and `SHIPPED-CODE-DEFECTS.md:41-45`; current branch still states it is unverified and only the dispatch decision is tested (`custom_components/oig_cloud/ai_task.py:116-126`). |
| R2-A3 - `oig_ai_status` sensor privacy boundary | CLOSED | `SCOPE-REVISION.md:R6.9` requires `oig_ai_status` and diagnostics to avoid raw key material, secret predicates, and unredacted raw exceptions (`SCOPE-REVISION.md:220-222`). No shipped `oig_ai_status` implementation exists to re-route. |
| R2 planned-risk row - cross-provider fallback consent | CLOSED | `SCOPE-REVISION.md:R7.11` requires fallback to stay within the selected provider by default, requires explicit stored consent for `ai_task` to backend and Groq to NVIDIA crossings, and overrides old `F1-DESIGN.md` fallback text (`SCOPE-REVISION.md:301-305`). |
| AKEY-R6-001 - `/solar_test` secrecy | CLOSED | `SCOPE-REVISION.md:R7.3` binds logs, REST payloads, diagnostics, and DOM-visible errors to classified-only output for Forecast.Solar and Solcast, with a seeded secret/URL/raw-exception falsifier (`SCOPE-REVISION.md:253-257`). |
| AKEY-R6-002 - solar credential private storage, clear, teardown, and backup hygiene | PARTIALLY-CLOSED | Secret fields are closed by `SCOPE-REVISION.md:R7.1` and `R7.2`: `solar_forecast_api_key`, `solcast_api_key`, and secret aliases must use per-entry `Store(..., private=True)`, `entry.options` may hold only metadata and `*_set`, and migration/journal/audit/diagnostics writes fail closed on secret key/value (`SCOPE-REVISION.md:240-251`). Still missing or contradictory: R7.7 requires credential-field persistence/remount, no exact solar store key is named, and `solcast_site_id` is not classified. See AKEY-R7-001 to AKEY-R7-003. |
| AKEY-R6-003 - replacement solar key verified before activation | CLOSED | `SCOPE-REVISION.md:R7.12` says a newly submitted solar key must pass `/solar_test` before becoming active, failed or skipped verification leaves the prior credential untouched, and provider switches needing different material fail unless replacement is present and verified (`SCOPE-REVISION.md:307-312`). |
| AKEY-R6-004 - explicit cross-provider fallback consent | CLOSED | `SCOPE-REVISION.md:R7.11` defines the consent boundary and falsifier: missing consent plus `ai_task` failure must make no outbound Groq/NVIDIA call and surface a refusal code (`SCOPE-REVISION.md:301-305`). |

Neutral R2 verification rows (`R2-S4`, `R2-T1`, `R2-A1`, `R2-P1`) remain non-defect observations and are not counted as prior findings needing closure.

## 2. New findings

| id | severity | file:clause | lifecycle step | what the spec leaves undefined | the test that would catch it | the exact spec text that would fix it |
|---|---|---|---|---|---|---|
| AKEY-R7-001 | CRITICAL | `SCOPE-REVISION.md:R7.1`, `SCOPE-REVISION.md:R7.7` | Entry and UI echo | R7.1 says solar secret fields must be persisted only in private per-entry stores and `entry.options` may hold only non-secret metadata plus `*_set` booleans (`SCOPE-REVISION.md:241-245`). R7.7 then requires the Step-2 save request to include and persist all solar form keys including the credential field, requires Step-2 values to be restored from persisted config on remount, and gives a falsifier where `solar_forecast_api_key` being absent after remount fails the test (`SCOPE-REVISION.md:277-281`). As written, an implementer can satisfy R7.7 only by echoing key material through onboarding state, REST, or DOM, contradicting R7.1. | Seed `solar_forecast_api_key="fs_secret_123456789"`, save Step 2, close/remount the wizard, and capture `/onboarding`, `/module_config`, rendered DOM, and input `.value`. Assert `fs_secret_123456789` is absent everywhere outside the private credential store; only `solar_forecast_api_key_set=true` and a blank password field with configured placeholder may be visible. The test must fail if the key is returned, prefilled, logged, or serialized in draft state. | `R7.7a - Secret fields are never remounted as values. Step-2 persistence MUST save non-secret solar fields and MUST save only secret candidate material into the private solar credential store after successful verification. Remount MUST NOT return or prefill solar_forecast_api_key or solcast_api_key. Remount may expose only solar_forecast_api_key_set and solcast_api_key_set booleans plus blank password controls. The R7.7 falsifier MUST assert latitude is restored and the secret value is absent, not present.` |
| AKEY-R7-002 | MAJOR | `SCOPE-REVISION.md:R7.1`, `SCOPE-REVISION.md:R6.7` | Storage and deletion | R6.7 names the AI store key `oig_cloud.ai_<entry_id>` for teardown assertions (`SCOPE-REVISION.md:210-213`), but R7.1 only says solar secrets use a per-entry `Store(..., private=True)` and does not name the solar credential store key or collision rule (`SCOPE-REVISION.md:241-245`). Deletion tests cannot prove the exact persistent artifact is gone, and an implementation could use a shared store, a non-namespaced key, or a key that collides with `oig_cloud.migration_backup_<entry_id>`. | Monkeypatch `Store` construction and remove an entry seeded with both solar provider keys. Assert the only solar credential store key is the exact approved per-entry name, every construction has `private=True`, that store is deleted on clear/provider-switch/removal, `oig_cloud.ai_<entry_id>` is deleted for AI clear/removal, and `oig_cloud.migration_backup_<entry_id>` contains no secret key or value. The test must fail on any shared solar credential store or backup-key collision. | `R7.1a - Exact credential store names. AI credentials MUST use Store(hass, 1, f"oig_cloud.ai_{entry_id}", private=True). Solar credentials MUST use Store(hass, 1, f"oig_cloud.solar_{entry_id}", private=True). No credential store may share or prefix-collide with oig_cloud.migration_backup_<entry_id>. Clear, provider switch cleanup, and config-entry removal MUST delete oig_cloud.ai_<entry_id> and oig_cloud.solar_<entry_id>; tests MUST assert those exact store keys are absent and migration backup contains no secret fields.` |
| AKEY-R7-003 | MAJOR | `SCOPE-REVISION.md:R7.1`, `SCOPE-REVISION.md:R7.3`, `docs/redesign_2026_07/PLAN-3.6-SPEC.md:AK-2` | Storage, use, and observability | `solcast_site_id` is required in the solar test request as a selected provider credential or site id (`docs/redesign_2026_07/PLAN-3.6-SPEC.md:44-49`) and is currently a non-secret registry field (`custom_components/oig_cloud/config_registry.py:178-183`), but R7.1 defines private storage only for `secret=True` fields and R7.3 bans raw URL/body leakage without classifying the site id itself (`SCOPE-REVISION.md:241-257`). The spec still does not say whether `solcast_site_id` may be stored in `entry.options`, diagnostics, migration backups, status surfaces, or AI prompts as non-secret metadata. | Seed `solcast_site_id="site_leak_12345"` with a Solcast key, run module config read, onboarding remount, diagnostics export, migration backup/journal writes, `/solar_test` failure, and `validate_config` prompt serialization. Assert the result matches the chosen classification: if sensitive, the value is absent from all non-private stores and surfaces; if non-secret, it is still excluded from AI prompts and raw provider URLs/errors. | `R7.1b - Solcast site id classification. solcast_site_id is classified explicitly as either non-secret provider metadata or sensitive account/site identifier. If sensitive, it MUST follow the private-store and redaction lifecycle for solar secrets. If non-secret, it MAY remain in entry.options and admin-only config payloads, but MUST NOT appear in AI prompt payloads, diagnostics, logs, raw provider URLs, or non-admin status surfaces. Tests MUST cover both persistence and outbound prompt/diagnostic boundaries for a seeded solcast_site_id.` |
| AKEY-R7-004 | MINOR | `docs/redesign_2026_07/PLAN-3.6-SPEC.md:AK-2` | Verification | AK-2 says the UI-to-endpoint classified error shape and verification-before-replace rules are bound to `SCOPE-REVISION.md:R7.3` and `R7.11` (`docs/redesign_2026_07/PLAN-3.6-SPEC.md:52`). R7.11 is fallback consent, not replacement verification. The binding should cite R7.12, and the current reference can send an implementer to the wrong clause even though the next falsifier sentence is substantively correct (`docs/redesign_2026_07/PLAN-3.6-SPEC.md:53`). | Add a docs lint that finds `verification-before-replace` and asserts the same line cites `SCOPE-REVISION.md:R7.12`; fail if it cites only `R7.11`. | `In PLAN-3.6-SPEC.md AK-2, replace "SCOPE-REVISION.md: R7.3 and SCOPE-REVISION.md: R7.11" with "SCOPE-REVISION.md: R7.3 and SCOPE-REVISION.md: R7.12".` |

## 3. Could not establish

- No slow build or test gate was run, per brief.
- No `/api/oig_cloud/{box}/solar_test`, `config_migration.py`, D8 `validate_config`, or `oig_ai_status` implementation exists in this worktree; behavior above is assessed from binding text plus the shipped code paths likely to be reused.
- I could not establish the intended sensitivity class for `solcast_site_id`; no R7 clause states whether it is non-secret metadata or a sensitive account/site identifier.
