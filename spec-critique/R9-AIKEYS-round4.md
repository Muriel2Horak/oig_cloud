CRITICAL: 0 | MAJOR: 1 | MINOR: 0

# R9-AIKEYS round 4

## 1. Prior-finding verdicts

Verdict scope: CLOSED means the updated documents add a binding requirement or correctly route an already-shipped defect to `spec-critique/SHIPPED-CODE-DEFECTS.md`. CLOSED does not claim shipped code is fixed. Prior defect rows closed: 17/17.

| id | CLOSED/PARTIAL/OPEN | evidence `file:clause` |
|---|---|---|
| AIK-1 / R2-S1 - no `AiKeyStore` delete or integration teardown cleanup | CLOSED | `SCOPE-REVISION.md:R6.7` requires delete/clear semantics and integration removal cleanup for `oig_cloud.ai_<entry_id>` (`SCOPE-REVISION.md:210-213`). `SCOPE-REVISION.md:R8.7` names the exact AI store and deletion assertions (`SCOPE-REVISION.md:368-376`). SHIPPED-CODE routing remains correct: `spec-critique/SHIPPED-CODE-DEFECTS.md:11-15`. |
| AIK-2 / R2-S2 - provider change can keep a stale AI key | CLOSED | `SCOPE-REVISION.md:R6.7` requires options flow to fail rather than silently keep a mismatched key/provider pair (`SCOPE-REVISION.md:210-213`). `SCOPE-REVISION.md:R8.7` binds provider-switch deletion of the exact credential stores (`SCOPE-REVISION.md:368-376`). SHIPPED-CODE routing remains correct: `spec-critique/SHIPPED-CODE-DEFECTS.md:17-21`. |
| AIK-3 / R2-S3 - `/ai` stores replacement before verification | CLOSED | `SCOPE-REVISION.md:R6.7` requires key replacement to verify before switching the active key (`SCOPE-REVISION.md:210-213`). SHIPPED-CODE routing remains correct: `spec-critique/SHIPPED-CODE-DEFECTS.md:23-27`. |
| AIK-4 / R2-T2 - raw verify exception detail can reach REST/UI | CLOSED | `SCOPE-REVISION.md:R6.9` requires failing verify checks to keep provider key text out of REST payloads and diagnostics (`SCOPE-REVISION.md:220-222`). SHIPPED-CODE routing remains correct for the existing raw-detail path: `spec-critique/SHIPPED-CODE-DEFECTS.md:29-33`. |
| AIK-5 / R2-T3 - prompt `task` is free text | CLOSED | `SCOPE-REVISION.md:R6.8` requires `task` to be enum/constant and forbids raw `task` concatenation (`SCOPE-REVISION.md:215-218`). SHIPPED-CODE routing remains correct: `spec-critique/SHIPPED-CODE-DEFECTS.md:35-39`. |
| AIK-6 / R2-P2 - `validate_config` lacks typed anonymous collector and wire test | CLOSED | `SCOPE-REVISION.md:R6.8` requires collectors to exclude coordinates, `box_id`, `entity_id`, API keys, and PII, with a wire-payload falsifier (`SCOPE-REVISION.md:215-218`). `SCOPE-REVISION.md:R8.8` also excludes `solcast_site_id` from the AI prompt collector (`SCOPE-REVISION.md:378-383`). SPEC routing remains correct: `spec-critique/R6-CLASSIFICATION.md:67`. |
| AIK-7 / R2-P3 - `ai_task` delegation branch unverified | CLOSED | The defect is correctly routed to shipped code with runtime verification required before use (`spec-critique/SHIPPED-CODE-DEFECTS.md:41-45`). The spec-side prompt boundary is covered by `SCOPE-REVISION.md:R6.8` (`SCOPE-REVISION.md:215-218`). |
| R2-A3 - `oig_ai_status` sensor privacy boundary | CLOSED | `SCOPE-REVISION.md:R6.9` requires `oig_ai_status` and diagnostics to avoid raw key material, secret predicates, and unredacted raw exceptions (`SCOPE-REVISION.md:220-222`). The implementation handoff repeats the diagnostic redaction rule (`docs/redesign_2026_07/IMPLEMENTATION-BRIEF-EN.md:94-97`). |
| R2 planned-risk row - cross-provider fallback consent | CLOSED | `SCOPE-REVISION.md:R7.11` keeps fallback within the selected provider by default, requires explicit stored consent for `ai_task` to backend and Groq to NVIDIA crossings, and requires no outbound call without consent (`SCOPE-REVISION.md:301-305`). |
| AKEY-R6-001 - `/solar_test` secrecy | CLOSED | `SCOPE-REVISION.md:R7.3` binds logs, REST payloads, diagnostics, and DOM-visible errors to classified-only output for Forecast.Solar and Solcast and bans secret text, request URLs, request bodies, and raw exceptions (`SCOPE-REVISION.md:253-257`). `SCOPE-REVISION.md:R8.8` extends the same lifecycle to `solcast_site_id` (`SCOPE-REVISION.md:378-383`). |
| AKEY-R6-002 - solar credential private storage, clear, teardown, and backup hygiene | CLOSED | `SCOPE-REVISION.md:R7.1` requires solar secret fields to live only in per-entry private stores and out of `entry.options` except metadata and `*_set` booleans (`SCOPE-REVISION.md:240-245`). `SCOPE-REVISION.md:R7.2` fail-closes backup, journal, audit, and diagnostics writes containing secret keys or values (`SCOPE-REVISION.md:247-251`). `SCOPE-REVISION.md:R8.7` names `oig_cloud.solar_<entry_id>` and teardown semantics (`SCOPE-REVISION.md:368-376`). |
| AKEY-R6-003 - replacement solar key verified before activation | CLOSED | `SCOPE-REVISION.md:R7.12` requires a new solar key to pass `/solar_test` before activation, preserves the old credential on failed/skipped verification, and fails provider switches without verified replacement material (`SCOPE-REVISION.md:307-312`). `PLAN-3.6-SPEC.md` binds AK-2 to R7.12 and fails if a replacement key is sent without successful `/solar_test` (`docs/redesign_2026_07/PLAN-3.6-SPEC.md:52-53`). |
| AKEY-R6-004 - explicit cross-provider fallback consent | CLOSED | `SCOPE-REVISION.md:R7.11` defines the consent boundary and seeded falsifier: missing consent plus `ai_task` failure must make no outbound Groq/NVIDIA call and must surface a refusal code (`SCOPE-REVISION.md:301-305`). |
| AKEY-R7-001 - R7.7 secret-remount contradiction | CLOSED | `SCOPE-REVISION.md:R8.1` supersedes the reversed R7.7/R7.8 falsifiers, requires non-secret Step-2 values on remount, forbids raw solar secret fields in UI, `entry.options`, `/onboarding` request/response, and saved draft state, and allows only `*_set` booleans plus blank password controls (`SCOPE-REVISION.md:319-330`). The handoff repeats the polarity override (`docs/redesign_2026_07/IMPLEMENTATION-BRIEF-EN.md:165-167`). |
| AKEY-R7-002 - exact credential store names and teardown semantics | CLOSED | `SCOPE-REVISION.md:R8.7` binds exact store names `oig_cloud.ai_<entry_id>` and `oig_cloud.solar_<entry_id>`, requires `private=True`, forbids collision with `oig_cloud.migration_backup_<entry_id>`, and requires clear/provider-switch/entry-removal deletion assertions (`SCOPE-REVISION.md:368-376`). `SCOPE-REVISION.md:R9.3` adds a teardown budget and asserts both stores are gone (`SCOPE-REVISION.md:423-426`). |
| AKEY-R7-003 - `solcast_site_id` classification | CLOSED | `SCOPE-REVISION.md:R8.8` classifies `solcast_site_id` as a sensitive account/site identifier and binds it to private storage, `/solar_test` redaction, AI prompt exclusion, diagnostics/log redaction, and raw URL exclusion (`SCOPE-REVISION.md:378-383`). `SCOPE-REVISION.md:R9.1` also forbids `solcast_site_id` in non-admin `/onboarding` refusal outputs and admin positive raw-value outputs (`SCOPE-REVISION.md:411-412`). |
| AKEY-R7-004 - wrong AK-2 citation to R7.11 instead of R7.12 | CLOSED | `SCOPE-REVISION.md:R8.9` requires AK-2 to cite R7.3 and R7.12 and requires classified Step-2 failure not to block `wizard-next` or `wizard-skip` (`SCOPE-REVISION.md:385-390`). The edited plan cites R7.12 on the verification-before-replace line (`docs/redesign_2026_07/PLAN-3.6-SPEC.md:52-53`). |

Neutral R2 verification rows (`R2-S4`, `R2-T1`, `R2-A1`, `R2-P1`) remain non-defect observations.

## 2. New findings

| id | severity | `file:clause` | what is wrong | the falsification test that would catch it | the exact spec text that would fix it |
|---|---|---|---|---|---|
| R9-AKEY-001 | MAJOR | `SCOPE-REVISION.md:R9.1` (`SCOPE-REVISION.md:397-413`), `custom_components/oig_cloud/api/ha_rest_api.py:1330`, `custom_components/oig_cloud/api/ha_rest_api.py:1357-1358`, `custom_components/oig_cloud/api/ha_rest_api.py:1360-1424` | `R9.1` declares the `/api/oig_cloud/**` auth matrix CLOSED and says every shipped endpoint under the prefix must appear before it may ship, but the existing AI key state/verify route `/api/oig_cloud/{box}/ai` is omitted. An implementer can satisfy the listed R9 matrix tests while leaving the AI key route outside the uniform non-admin refusal, unsupported-method, body-acceptance, safe-state-shape, and provider-verify failure-output coverage. The current shipped route exists and carries the AI key lifecycle surface: `GET` returns state from `AiKeyStore`, and `POST` accepts `provider` plus `api_key` for verification. | Add `/ai` to the endpoint-matrix fuzz test. As an authenticated non-admin, call `GET`, `POST`, `DELETE`, `PUT`, and `PATCH /api/oig_cloud/{box}/ai` for one existing box and one guessed/missing box; assert identical safe refusal for existing and missing boxes, no key state, and no request body accepted before refusal. As admin, assert `GET` returns only `provider`, `key_set`, and `verified`; `POST` accepts only `provider` and `api_key`, verifies before active promotion, never returns raw/masked key text, and on provider failure emits only classified output in REST, DOM, logs, and diagnostics; clear/delete removes `oig_cloud.ai_<entry_id>`. | Add an `R9.1` matrix row for `/api/oig_cloud/{box}/ai` with allowed methods `GET`, `POST`, and `DELETE`. Admin outcome: `GET` returns only `{provider, key_set, verified}`; `POST` accepts only `provider` plus `api_key`, sends the provider probe with key material only in the provider `Authorization` header, and promotes a replacement only after successful verification; `DELETE` clears `oig_cloud.ai_<entry_id>`. Authenticated non-admin outcome: `403` before any body is accepted and before provider, `key_set`, `verified`, `api_key`, masked key text, or provider probe output is returned; refusal shape is identical for existing and missing boxes. Add: unsupported methods for `/ai` return `405` with no state mutation and no outbound provider call. `/ai` failure handling MUST be classified-only in REST, DOM, logs, and diagnostics and MUST NOT include raw key text, masked key tail, `Authorization` header, request body, provider URL carrying key material, or raw upstream exception string. |

## 3. Bucket routing

| bucket | findings |
|---|---|
| SPEC | New: `R9-AKEY-001`. Prior SPEC closures: `AIK-6`, `R2-A3`, `R2 planned-risk row`, `AKEY-R6-001`, `AKEY-R6-002`, `AKEY-R6-003`, `AKEY-R6-004`, `AKEY-R7-001`, `AKEY-R7-002`, `AKEY-R7-003`, `AKEY-R7-004`. |
| SHIPPED-CODE | Prior routed defects only: `AIK-1`, `AIK-2`, `AIK-3`, `AIK-4`, `AIK-5`, `AIK-7` (`spec-critique/SHIPPED-CODE-DEFECTS.md:11-45`). No new SHIPPED-CODE finding in this round. |

## 4. What I could not establish

- I did not establish future runtime behavior for the R9 fixes; this is a spec review. Current code was inspected only to prove the existing `/api/oig_cloud/{box}/ai` route is a shipped endpoint (`custom_components/oig_cloud/api/ha_rest_api.py:1330`, `custom_components/oig_cloud/api/ha_rest_api.py:1553`, `custom_components/oig_cloud/api/ha_rest_api.py:1567`).
- I did not run a build or test suite, per brief.
- I did not edit `SCOPE-REVISION.md`, `PLAN-3.6-SPEC.md`, `IMPLEMENTATION-BRIEF-EN.md`, shipped code, or any file other than this report.
