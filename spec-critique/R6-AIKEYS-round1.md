# R6-AIKEYS round 1 - CRITICAL 1 / MAJOR 3 / MINOR 0

## 1. Prior-finding verdicts

Verdict scope: CLOSED means the updated documents either add a binding requirement or correctly route an already-shipped defect to `SHIPPED-CODE-DEFECTS.md`. It does not claim the shipped code is fixed.

| id | verdict | evidence |
|---|---|---|
| AIK-1 / R2-S1 - no `AiKeyStore` delete or integration teardown cleanup | CLOSED | `SCOPE-REVISION.md:R6.7` requires delete/clear semantics and integration removal cleanup for `oig_cloud.ai_<entry_id>` (`SCOPE-REVISION.md:210-213`). SHIPPED-CODE routing confirmed: `SHIPPED-CODE-DEFECTS.md:11-15`; current store has set/get/mark/state only (`custom_components/oig_cloud/ai/key_store.py:43-67`). |
| AIK-2 / R2-S2 - provider change can keep a stale key | CLOSED | `SCOPE-REVISION.md:R6.7` requires options flow to fail rather than keep a mismatched key/provider pair (`SCOPE-REVISION.md:210-213`). SHIPPED-CODE routing confirmed: `SHIPPED-CODE-DEFECTS.md:17-21`; current options flow stores a new key only when nonblank and still updates provider/options (`custom_components/oig_cloud/config/steps.py:3599-3612`). |
| AIK-3 / R2-S3 - `/ai` stores replacement before verification | CLOSED | `SCOPE-REVISION.md:R6.7` requires verification before active-key switch (`SCOPE-REVISION.md:210-213`). SHIPPED-CODE routing confirmed: `SHIPPED-CODE-DEFECTS.md:23-27`; current REST path calls `async_set_key` before `async_verify_key` (`custom_components/oig_cloud/api/ha_rest_api.py:1394-1424`). |
| AIK-4 / R2-T2 - raw verify exception detail can reach REST/UI | CLOSED | `SCOPE-REVISION.md:R6.9` bans unredacted raw provider exceptions in execution/diagnostics (`SCOPE-REVISION.md:220-222`). SHIPPED-CODE routing confirmed: `SHIPPED-CODE-DEFECTS.md:29-33`; current response returns `detail: str(err)` (`custom_components/oig_cloud/api/ha_rest_api.py:1411-1420`). |
| AIK-5 / R2-T3 - prompt `task` is free text | CLOSED | `SCOPE-REVISION.md:R6.8` requires `task` to be enum/constant, never concatenated raw text (`SCOPE-REVISION.md:215-218`). SHIPPED-CODE routing confirmed: `SHIPPED-CODE-DEFECTS.md:35-39`; current prompt prepends `task={task}` (`custom_components/oig_cloud/ai/backends.py:60-72`). |
| AIK-6 / R2-P2 - `validate_config` lacks typed anonymous collector and wire test | CLOSED | `SCOPE-REVISION.md:R6.8` requires validate-config collectors to exclude coordinates, `box_id`, `entity_id`, API keys and PII, with a wire-payload falsification test (`SCOPE-REVISION.md:215-218`). SPEC routing in `R6-CLASSIFICATION.md:67` is confirmed. |
| AIK-7 / R2-P3 - `ai_task` delegation branch unverified | CLOSED | SHIPPED-CODE routing confirmed: `SHIPPED-CODE-DEFECTS.md:41-45`; current branch explicitly says it is unverified and only dispatch decision is tested (`custom_components/oig_cloud/ai_task.py:116-126`). |
| R2 planned-risk row - fallback/status sensor privacy boundary | PARTIALLY-CLOSED | Status/diagnostics half is closed by `SCOPE-REVISION.md:R6.9` (`SCOPE-REVISION.md:220-222`). Cross-provider fallback consent is still missing; see AKEY-R6-004. |

## 2. New findings

| id | severity | file:clause | lifecycle step | what the spec leaves undefined | the test that would catch it | exact spec text that would fix it |
|---|---|---|---|---|---|---|
| AKEY-R6-001 | CRITICAL | `SCOPE-REVISION.md:R6.5`, `SCOPE-REVISION.md:R6.9` | Solar verification and observability | `/solar_test` is required to send the current key/site id (`SCOPE-REVISION.md:200-203`), but R6.5 only says errors are safe for display and R6.9 only tests a bad AI provider key (`SCOPE-REVISION.md:220-222`). It does not bind solar validation logs, REST errors, DOM errors, or raw provider URL/exception redaction. This is not theoretical: current solar key validation builds the key into the URL and logs the first 50 chars of that URL (`custom_components/oig_cloud/config/validation.py:72-77`). | Seed `solar_forecast_api_key="fs_secret_123456789"` or `solcast_api_key="sc_secret_123456789"`, force `/solar_test` provider 401 and a provider exception whose string contains the request URL, then assert REST body, rendered DOM, and captured logs contain neither key, key prefix, request URL path segment, nor raw exception text. | `R6.5a - Solar test secrecy. The /solar_test endpoint and every solar provider verification helper MUST treat solar provider credentials as secret material. Logs, REST payloads, diagnostics, and DOM-visible errors MUST contain only classified error codes and redacted provider names; they MUST NOT contain solar_forecast_api_key, solcast_api_key, solcast_site_id when classified sensitive, provider request URLs, Authorization headers, request bodies, or raw upstream exception strings. Regression tests MUST capture logs plus REST and DOM output for failing Forecast.Solar and Solcast calls.` |
| AKEY-R6-002 | MAJOR | `SCOPE-REVISION.md:R6.1`, `SCOPE-REVISION.md:R6.7` | Storage and deletion | R6.1 protects only migration backups (`SCOPE-REVISION.md:179-183`) and R6.7 covers only `AiKeyStore` (`SCOPE-REVISION.md:210-213`). The active solar credentials are not assigned private per-entry storage, explicit clear semantics, or entry teardown requirements. Current code marks `solar_forecast_api_key` and `solcast_api_key` as registry secrets (`custom_components/oig_cloud/config_registry.py:178-181`) but stores and reads them from `entry.options` (`custom_components/oig_cloud/entities/solar_forecast_sensor.py:640-642`, `:725-726`). | Create an entry with `solar_forecast_api_key`, `solcast_api_key`, and `solcast_site_id`; run explicit clear, provider switch, and config-entry removal. Assert `entry.options` lacks `solar_forecast_api_key` and `solcast_api_key`, any private solar credential store for the entry is removed, and migration backup `oig_cloud.migration_backup_<entry_id>` contains no secret fields. | `R6.7a - Solar credential lifecycle. Solar provider credentials MUST have an explicit lifecycle equivalent to AiKeyStore: private per-entry storage or an explicitly justified options-storage exception with diagnostics/export redaction; explicit clear; provider-switch cleanup; and config-entry removal cleanup. The exact keys are solar_forecast_api_key and solcast_api_key; solcast_site_id MUST be classified as either non-secret identifier or sensitive account/site identifier and handled consistently. Tests MUST assert those keys are absent from active options/backups after clear and teardown.` |
| AKEY-R6-003 | MAJOR | `SCOPE-REVISION.md:R6.5` | Solar key rotation | R6.5 adds a rendered `[Otestovat]` call, but it does not say that a new solar key must be verified before replacing the active key. Current REST save merges accepted solar values into options after shape/cross-field validation (`custom_components/oig_cloud/api/ha_rest_api.py:1284-1294`), not after upstream key verification. A bad replacement can therefore become active if the implementation treats `[Otestovat]` as optional. | Start with a working Forecast.Solar or Solcast key, submit a new wrong key through the wizard/settings save path, force `/solar_test` to return `auth`, and assert the old active key remains in use and the candidate is not written to active options/store. Also assert a provider switch without a verified replacement fails with an explicit user action state. | `R6.5b - Solar credential rotation. A newly submitted solar provider key MUST be verified successfully by /solar_test before it replaces the active credential. Failed or skipped verification MUST leave the previous active credential untouched and return an explicit user-action state. Provider switches that require different credential material MUST fail unless the replacement credential is present and verified in the same flow.` |
| AKEY-R6-004 | MAJOR | `SCOPE-REVISION.md:R6.8`, `SCOPE-REVISION.md:R6.9` | AI use and fallback | R6.8 hardens prompt schema and R6.9 hardens status/diagnostics, but neither defines the consent boundary for fallback across providers. The old risk remains: a user selecting `ai_task` or one keyed provider can have the same prompt sent to Groq/NVIDIA or another provider unless fallback is constrained. This is especially ambiguous because older design text allows `ai_task` failure to fall back to the keyed backend (`docs/redesign_2026_07/F1-DESIGN.md:57-59`) while R6 does not override that rule. | Configure provider `ai_task` plus a stored Groq/NVIDIA key, force host `ai_task` failure, and assert no Groq/NVIDIA HTTP request is made unless an explicit cross-provider fallback consent flag is set. Repeat for Groq -> NVIDIA fallback. | `R6.8a - Provider consent boundary. Fallback MUST stay within the user's selected provider and that provider's bundled model list by default. Crossing from ai_task to Groq/NVIDIA, or from Groq to NVIDIA, requires an explicit stored consent flag and a user-visible disclosure. Without that consent, provider failure returns a classified error and sends no prompt to another provider.` |

## 3. Could not establish

- No `diagnostics.py` implementation exists in the current tree (`rg --files /repos/wt-oig-p3impl | rg 'diagnostics|diagnostic'` returned no files), so diagnostics behavior is only assessable from spec text, not shipped implementation.
- No `/api/oig_cloud/{box}/solar_test`, `config_migration.py`, D8 `validate_config` task implementation, or `oig_ai_status` implementation is present in this worktree. Findings above therefore target the updated binding text and the code paths that future work is likely to reuse.
- No slow build or test gate was run, per brief.
